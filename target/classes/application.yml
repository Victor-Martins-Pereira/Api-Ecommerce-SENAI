spring:
  application:
    name: ativiade senai
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerceAva?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create
    show-sql: true
  data:
    jdbc:
      dialect: mysql